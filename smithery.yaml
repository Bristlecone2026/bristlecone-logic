startCommand:
  type: "http"
  url: "https://bristleconelogic.com/mcp"
  configSchema:
    type: object
    properties:
      apiKey:
        type: string
        title: "API Key"
        description: "Bristlecone API Key (optional for free trial)"
