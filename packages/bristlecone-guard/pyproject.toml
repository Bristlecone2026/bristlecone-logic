[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "bristlecone-guard"
version = "0.1.0"
description = "Deterministic runtime guardrails and stdio proxy for Bristlecone Logic MCP"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Bristlecone Logic", email = "ops@bristleconelogic.com" }]
dependencies = []

[project.scripts]
bristlecone-guard = "bristlecone_guard.cli:main"
